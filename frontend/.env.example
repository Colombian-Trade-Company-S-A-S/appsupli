# URL base de la API. Si usas el proxy de Vite deja "/api".
VITE_API_URL=/api

# Target del proxy en desarrollo (opcional, apunta al backend local)
VITE_API_PROXY_TARGET=http://localhost:8000

VITE_APP_NAME=Supli
