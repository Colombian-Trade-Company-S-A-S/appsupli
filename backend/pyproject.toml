[tool.ruff]
line-length = 100
target-version = 'py313'
exclude = ['env', '.venv', 'venv', 'migrations']

[tool.ruff.lint]
select = ['E', 'F', 'I', 'UP', 'B', 'DJ', 'C4']
ignore = ['B008']

[tool.ruff.lint.isort]
known-first-party = ['apps', 'config']

[tool.ruff.format]
quote-style = 'single'
